<%= form_for(vehicle_condition) do |f| %>
  <% if vehicle_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_condition.errors.count, "error") %> prohibited this vehicle_condition from being saved:</h2>

      <ul>
      <% vehicle_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
