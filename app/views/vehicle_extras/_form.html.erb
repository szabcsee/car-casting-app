<%= form_for(vehicle_extra) do |f| %>
  <% if vehicle_extra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_extra.errors.count, "error") %> prohibited this vehicle_extra from being saved:</h2>

      <ul>
      <% vehicle_extra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :vehicle_type_id %>
    <%= f.text_field :vehicle_type_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
