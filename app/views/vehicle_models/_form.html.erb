<%= form_for(vehicle_model) do |f| %>
  <% if vehicle_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_model.errors.count, "error") %> prohibited this vehicle_model from being saved:</h2>

      <ul>
      <% vehicle_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :vehicle_brand_id %>
    <%= f.text_field :vehicle_brand_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
