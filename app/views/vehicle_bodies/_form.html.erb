<%= form_for(vehicle_body) do |f| %>
  <% if vehicle_body.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_body.errors.count, "error") %> prohibited this vehicle_body from being saved:</h2>

      <ul>
      <% vehicle_body.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
